#include<bits/stdc++.h>
using namespace std;
int factorial(int n)
{
     if(n<=1)
         return 1;
     else 
         return n*fact(n-1);
}
int main()
{
     int n;
     cout<<"enter the number:"<<n;
     cin>>n;
     cout<"factorial of n is:"<<factorial(n);
}     
    
